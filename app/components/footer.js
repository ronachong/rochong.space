console.log("loading footer");

import React from 'react';

const styles = {
    footer : {
        //
    },
};

const Footer = (props) => (
    <footer style={styles.footer}>
        Footer text here.
    </footer>
);

export default Footer;
